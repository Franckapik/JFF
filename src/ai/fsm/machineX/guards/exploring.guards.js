/**
 * ==========================================================================
 * XSTATE EXPLORING GUARDS - Pas de guards spécifiques (voir discoveryGuards)
 * ==========================================================================
 * 
 * Tous les guards nécessaires à l'état `exploring` sont déjà couverts par
 * `discoveryGuards` (needsExploration, hasUnexploredAreas, etc.) ou par les
 * guards de base (safety, efficiency).
 * 
 * Si une logique spécifique à `exploring` est requise à l'avenir, l'implémenter ici.
 */

// Aucun guard spécifique à migrer pour l'état exploring à ce stade.
